# An Example of a string comparison operation 

$0 >= "M"

	# Rather interesting application of the built in string operations, this selects all lines where the first field begins with a character "greater than" the letter M, basically all letters that would come *after* M in the vocab
	# awk -f SAMPLES/2-4.awk DATA/countries

