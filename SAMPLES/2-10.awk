# Reassign a field value --> showcasing that field values are just regular variables (as are the upper-case built in values)

{
	$2 = $2/1000; 
	$4 = "EARF"
	$5 = ""
	print;
} 


	# awk -f SAMPLES/2-10.awk DATA/countries 
